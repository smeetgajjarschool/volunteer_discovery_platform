<script
  src="https://code.jquery.com/jquery-3.2.1.min.js"
  integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4="
  crossorigin="anonymous"></script>


<style>
  #time-range p {
      font-family:"Arial", sans-serif;
      font-size:14px;
      color:#333;
  }
  #time-range {
    width: 400px;
  }
</style>


<head>
<style>
  html,
  body,
  #map-canvas {
    margin: 0;
    padding: 0;
    height: 100%;
  }
</style>
</head>

<h2 class="page-header"></h2>
{{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}

{{#if volunteer}}
  <div id="volunteer_form">
  <form method="post" action="/profile/create">
     	<div class="form-group">
      	<label>Name</label>
      	<input type="text" class="form-control" placeholder="Name" name="name" value={{user.name}}>
    	</div>

    	<div class="form-group">
      	<label>Date of Birth</label>
      	<input type="date" class="form-control" placeholder="Date of Birth" name="dob">
    	</div>

    	<div class="form-group">
     		<div class="interests_wrapper">
  			<label>Interests</label>
  	    	<input type="text" class="form-control" placeholder="Interest" name="interests[]"><br>
    		</div>
  	    <button class="add_interest_button btn">Add Another Interest</button>
    	</div>

      <div class="form-group">
          <div class="skills_wrapper">
  			<label>Skills or Experiences</label>
  	    	<input type="text" class="form-control" placeholder="Skill or Experience" name="skills[]"><br>
    		</div>
  	    <button class="add_skill_button btn">Add Another Skill or Experience</button>
    	</div>


    	<div class="form-group">
          <div id="time-range">
            <br><label>Availability:</label><br>

            <input type="checkbox" id="checkbox-monday" name="checkbox-monday" value="available-monday">
            <span>Monday</span>
            <div id="available-monday-input-slider">
            <input type="text" class="slider-time-monday" name="availability_monday_from" value = '' readonly>
            <input type="text" class="slider-time2-monday" name="availability_monday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-monday"></div>
            </div></div>
            <br>
            
            <input type="checkbox" id="checkbox-tuesday" name="checkbox-tuesday" value="available-tuesday">
            <span>Tuesday</span>
            <div id="available-tuesday-input-slider">
            <input type="text" class="slider-time-tuesday" name="availability_tuesday_from" value = '' readonly>
            <input type="text" class="slider-time2-tuesday" name="availability_tuesday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-tuesday"></div>
            </div></div>
            <br>

            <input type="checkbox" id="checkbox-wednesday" name="checkbox-wednesday" value="available-wednesday">
            <span>Wednesday</span>
            <div id="available-wednesday-input-slider">
            <input type="text" class="slider-time-wednesday" name="availability_wednesday_from" value = '' readonly>
            <input type="text" class="slider-time2-wednesday" name="availability_wednesday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-wednesday"></div>
            </div></div>
            <br>

            <input type="checkbox" id="checkbox-thursday" name="checkbox-thursday" value="available-thursday">
            <span>Thursday</span>
            <div id="available-thursday-input-slider">
            <input type="text" class="slider-time-thursday" name="availability_thursday_from" value = '' readonly>
            <input type="text" class="slider-time2-thursday" name="availability_thursday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-thursday"></div>
            </div></div>
            <br>

            <input type="checkbox" id="checkbox-friday" name="checkbox-friday" value="available-friday">
            <span>Friday</span>
            <div id="available-friday-input-slider">
            <input type="text" class="slider-time-friday" name="availability_friday_from" value = '' readonly>
            <input type="text" class="slider-time2-friday" name="availability_friday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-friday"></div>
            </div></div>
            <br>

            <input type="checkbox" id="checkbox-saturday" name="checkbox-saturday" value="available-saturday">
            <span>Saturday</span>
            <div id="available-saturday-input-slider">
            <input type="text" class="slider-time-saturday" name="availability_saturday_from" value = '' readonly>
            <input type="text" class="slider-time2-saturday" name="availability_saturday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-saturday"></div>
            </div></div>
            <br>

            <input type="checkbox" id="checkbox-sunday" name="checkbox-sunday" value="available-sunday">
            <span>Sunday</span>
            <div id="available-sunday-input-slider">
            <input type="text" class="slider-time-sunday" name="availability_sunday_from" value = '' readonly>
            <input type="text" class="slider-time2-sunday" name="availability_sunday_to" value = '' readonly> 
            <br><br>
            <div class="sliders_step1">
                <div class="slider-range" id="slider-range-sunday"></div>
            </div></div>
            <br>

          </div>
        </div>

    <button type="submit" class="btn btn-default">Submit</button>
  </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.2.1.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>


  <script>
  	$(document).ready(function() {
  	    var max_fields      = 5; //maximum input boxes allowed
  	    var wrapper         = $(".interests_wrapper"); //Fields wrapper
  	    var add_button      = $(".add_interest_button"); //Add button ID
  	    
  	    var x = 1; //initial text box count
  	    $(add_button).click(function(e){ //on add input button click
  	        e.preventDefault();
  	        if(x < max_fields){ //max input box allowed
  	            x++; //text box increment
  	            $(wrapper).append('<div><input type="text" class="form-control" placeholder="Interest" name="interests[]"/><a href="#" class="remove_interest btn-link">Remove</a></div>'); //add input box
  	        }
  	    });
  	    
  	    $(wrapper).on("click",".remove_interest", function(e){ //user click on remove text
  	        e.preventDefault(); $(this).parent('div').remove(); x--;
  	    })
  	});

  	$(document).ready(function() {
  	    var max_fields      = 5; //maximum input boxes allowed
  	    var wrapper         = $(".skills_wrapper"); //Fields wrapper
  	    var add_button      = $(".add_skill_button"); //Add button ID
  	    
  	    var x = 1; //initial text box count
  	    $(add_button).click(function(e){ //on add input button click
  	        e.preventDefault();
  	        if(x < max_fields){ //max input box allowed
  	            x++; //text box increment
  	            $(wrapper).append('<div><input type="text" class="form-control" placeholder="Skill or Experience" name="skills[]"/><a href="#" class="remove_skill btn-link">Remove</a></div>'); //add input box
  	        }
  	    });
  	    
  	    $(wrapper).on("click",".remove_skill", function(e){ //user click on remove text
  	        e.preventDefault(); $(this).parent('div').remove(); x--;
  	    })
  	});
  </script>

  <script>
  	$(document).ready(function(){

   $("#slider-range-monday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          //$('.slider-time-monday').html(hours1 + ':' + minutes1);
          $('.slider-time-monday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-monday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-monday').change(function(){
      if (this.checked) {
          $('#available-monday-input-slider').fadeIn('fast');            
          $('.slider-time-monday').val("9:00 AM");
          $('.slider-time2-monday').val("5:00 PM");
          $('#slider-range-monday').slider('values', [540, 1020]);
        }
      else {
          $('#available-monday-input-slider').fadeOut('fast');
          $('.slider-time-monday').val("");
          $('.slider-time2-monday').val("");
      }                   
  });

  $("#slider-range-tuesday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-tuesday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-tuesday').val(hours2 + ':' + minutes2);    }
  });

  $('#checkbox-tuesday').change(function(){
      if (this.checked) {
          $('#available-tuesday-input-slider').fadeIn('fast');            
          $('.slider-time-tuesday').val("9:00 AM");
          $('.slider-time2-tuesday').val("5:00 PM");
          $('#slider-range-tuesday').slider('values', [540, 1020]);
        }
      else {
          $('#available-tuesday-input-slider').fadeOut('fast');
          $('.slider-time-tuesday').val("");
          $('.slider-time2-tuesday').val("");
      }                   
  });

  $("#slider-range-wednesday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-wednesday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-wednesday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-wednesday').change(function(){
      if (this.checked) {
          $('#available-wednesday-input-slider').fadeIn('fast');            
          $('.slider-time-wednesday').val("9:00 AM");
          $('.slider-time2-wednesday').val("5:00 PM");
          $('#slider-range-wednesday').slider('values', [540, 1020]);
        }
      else {
          $('#available-wednesday-input-slider').fadeOut('fast');
          $('.slider-time-wednesday').val("");
          $('.slider-time2-wednesday').val("");
      }                   
  });

  $("#slider-range-thursday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-thursday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-thursday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-thursday').change(function(){
      if (this.checked) {
          $('#available-thursday-input-slider').fadeIn('fast');            
          $('.slider-time-thursday').val("9:00 AM");
          $('.slider-time2-thursday').val("5:00 PM");
          $('#slider-range-thursday').slider('values', [540, 1020]);
        }
      else {
          $('#available-thursday-input-slider').fadeOut('fast');
          $('.slider-time-thursday').val("");
          $('.slider-time2-thursday').val("");
      }                   
  });

  $("#slider-range-friday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-friday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-friday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-friday').change(function(){
      if (this.checked) {
          $('#available-friday-input-slider').fadeIn('fast');            
          $('.slider-time-friday').val("9:00 AM");
          $('.slider-time2-friday').val("5:00 PM");
          $('#slider-range-friday').slider('values', [540, 1020]);
        }
      else {
          $('#available-friday-input-slider').fadeOut('fast');
          $('.slider-time-friday').val("");
          $('.slider-time2-friday').val("");
      }                   
  });

  $("#slider-range-saturday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-saturday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-saturday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-saturday').change(function(){
      if (this.checked) {
          $('#available-saturday-input-slider').fadeIn('fast');            
          $('.slider-time-saturday').val("9:00 AM");
          $('.slider-time2-saturday').val("5:00 PM");
          $('#slider-range-saturday').slider('values', [540, 1020]);
        }
      else {
          $('#available-saturday-input-slider').fadeOut('fast');
          $('.slider-time-saturday').val("");
          $('.slider-time2-saturday').val("");
      }                   
  });

  $("#slider-range-sunday").slider({
      range: true,
      min: 0,
      max: 1440,
      step: 15,
      values: [540, 1020],
      slide: function (e, ui) {
          var hours1 = Math.floor(ui.values[0] / 60);
          var minutes1 = ui.values[0] - (hours1 * 60);

          if (hours1.length == 1) hours1 = '0' + hours1;
          if (minutes1.length == 1) minutes1 = '0' + minutes1;
          if (minutes1 == 0) minutes1 = '00';
          if (hours1 >= 12) {
              if (hours1 == 12) {
                  hours1 = hours1;
                  minutes1 = minutes1 + " PM";
              } else {
                  hours1 = hours1 - 12;
                  minutes1 = minutes1 + " PM";
              }
          } else {
              hours1 = hours1;
              minutes1 = minutes1 + " AM";
          }
          if (hours1 == 0) {
              hours1 = 12;
              minutes1 = minutes1;
          }

          $('.slider-time-sunday').val(hours1 + ':' + minutes1);

          var hours2 = Math.floor(ui.values[1] / 60);
          var minutes2 = ui.values[1] - (hours2 * 60);

          if (hours2.length == 1) hours2 = '0' + hours2;
          if (minutes2.length == 1) minutes2 = '0' + minutes2;
          if (minutes2 == 0) minutes2 = '00';
          if (hours2 >= 12) {
              if (hours2 == 12) {
                  hours2 = hours2;
                  minutes2 = minutes2 + " PM";
              } else if (hours2 == 24) {
                  hours2 = 11;
                  minutes2 = "59 PM";
              } else {
                  hours2 = hours2 - 12;
                  minutes2 = minutes2 + " PM";
              }
          } else {
              hours2 = hours2;
              minutes2 = minutes2 + " AM";
          }

          $('.slider-time2-sunday').val(hours2 + ':' + minutes2);
      }
  });

  $('#checkbox-sunday').change(function(){
      if (this.checked) {
          $('#available-sunday-input-slider').fadeIn('fast');            
          $('.slider-time-sunday').val("9:00 AM");
          $('.slider-time2-sunday').val("5:00 PM");
          $('#slider-range-sunday').slider('values', [540, 1020]);
        }
      else {
          $('#available-sunday-input-slider').fadeOut('fast');
          $('.slider-time-sunday').val("");
          $('.slider-time2-sunday').val("");
      }                   
  });

  });

  </script>

{{else if organization}}  
  <div id="organization_form">
  <form method="post" action="/profile/create">
     <div class="form-group">
      <label>Name</label>
      <input type="text" class="form-control" placeholder="Name" name="name" value={{user.name}}>
    </div>
     <div class="form-group">
      <label>Organization Name</label>
      <input type="text" class="form-control" placeholder="Organization Name" name="organization_name">
    </div>
    <div class="form-group">
      <label>Organization Role</label>
      <input type="text" class="form-control" placeholder="Role" name="role">
    </div>
    <button type="submit" class="btn btn-default">Save</button>
  </form>
  </div>

{{/if}}
